# RailGPT 算法建模说明

## 一、总体框架

"四层评估模型"——对任意一个应急调度方案，逐层计算：

```
输入：候选调度方案
  ↓
[第1层] 可行性校验 —— 6条高铁安全硬约束
  ↓ 通过
[第2层] 指标计算 —— 7个量化性能指标
  ↓
[第3层] Min-Max 归一化 —— 无量纲化
  ↓
[第4层] 加权综合打分 —— 排序推荐
  ↓
输出：最优方案
```

---

## 二、符号系统

| 符号 | 含义 |
|---|---|
| \( \mathcal{T} \) | 列车集合，\( T \) 为总数 |
| \( \mathcal{S} \) | 车站有序集合，\( s_1 \) 始发站，\( s_M \) 终到站 |
| \( \mathcal{E} \) | 区间集合（车站之间） |
| \( A_{i,j}^{plan} \) | 原始计划中列车 \( i \) 到达车站 \( j \) 的时刻 |
| \( D_{i,j}^{plan} \) | 原始计划中列车 \( i \) 从车站 \( j \) 出发的时刻 |
| \( A_{i,j}^{adj} \) | 调整方案中列车 \( i \) 到达车站 \( j \) 的时刻 |
| \( D_{i,j}^{adj} \) | 调整方案中列车 \( i \) 从车站 \( j \) 出发的时刻 |
| \( L_{e} \) | 区间 \( e \) 的长度 (km) |
| \( C_i \) | 列车 \( i \) 的额定载客人数 |
| \( V_{max} \) | 线路最高允许速度 (350 km/h) |

---

## 三、第1层：约束条件（Feasibility Check）

### 3.1 车站出发间隔约束

$$ |D_{i+1,j}^{adj} - D_{i,j}^{adj}| \geq H_{dep} $$

其中 \( H_{dep} \) 为最小出发间隔，取值 3-5 分钟。

### 3.2 车站到达间隔约束

$$ |A_{i+1,j}^{adj} - A_{i,j}^{adj}| \geq H_{arr} $$

其中 \( H_{arr} \) 为最小到达间隔，取值 3 分钟。

### 3.3 区间追踪间隔约束

$$ |D_{i+1,j}^{adj} - D_{i,j}^{adj}| \geq H_{sec} $$

其中 \( H_{sec} \) 为最小区间追踪间隔，取值 3 分钟。

### 3.4 最小停站时间约束

$$ D_{i,j}^{adj} - A_{i,j}^{adj} \geq T_{dwell}^{min} $$

其中 \( T_{dwell}^{min} = 2 \) 分钟（高铁中间站）。

### 3.5 区间运行时间约束

$$ A_{i,j+1}^{adj} - D_{i,j}^{adj} \geq T_{run}^{min}(j, j+1) $$

调度方案不得规定低于物理极限的运行时间。

---

## 四、第2层：评估指标（Performance Metrics）

### 4.1 总晚点时间 TDT

$$ M_{TDT} = \sum_{i=1}^{T} \max(0, A_{i,s_M}^{adj} - A_{i,s_M}^{plan}) $$

衡量所有列车在终到站的总延误分钟数。

### 4.2 平均晚点时间 ADT

$$ M_{ADT} = \frac{M_{TDT}}{N_{delayed}} $$

其中 \( N_{delayed} \) 为终到晚点大于 0 的列车数量。

### 4.3 旅客总延误 TPMD

$$ M_{TPMD} = \sum_{i=1}^{T} C_i \times \max(0, A_{i,s_M}^{adj} - A_{i,s_M}^{plan}) $$

将列车晚点乘以载客量，衡量对社会出行的影响。

### 4.4 受影响旅客总数 TAP

$$ M_{TAP} = \sum_{i=1}^{T} C_i \times \mathbb{I}(A_{i,s_M}^{adj} - A_{i,s_M}^{plan} > 0) $$

统计所有乘坐晚点列车的旅客总数。

### 4.5 终到晚点方差 VFD

$$ M_{VFD} = \frac{1}{N_{delayed}} \sum_{i=1}^{N_{delayed}} (d_i - \bar{d})^2 $$

衡量延误在各列车间分配的公平程度，方差越小越公平。

### 4.6 计划调整幅度 SD

$$ M_{SD} = \sum_{i=1}^{T} \sum_{j=1}^{M} |D_{i,j}^{adj} - D_{i,j}^{plan}| $$

衡量对原运行图的整体改动量，微调方案优于大改方案。

### 4.7 系统恢复时间 SRT

$$ M_{SRT} = t_{last\_delayed} - t_{event\_start} $$

从突发事件发生到最后一列车恢复正点所经过的总时长。

---

## 五、第3层：指标标准化

采用 Min-Max 归一化，将所有指标映射到 [0, 1] 区间：

$$ x_{k}^{norm} = \frac{\max(x_{k}) - x_{k}}{\max(x_{k}) - \min(x_{k})} $$

归一化后越接近 1 表示该指标表现越好（成本型指标）。

---

## 六、第4层：综合评分

$$ S_{plan} = \sum_{k=1}^{7} w_k \times x_{k}^{norm} $$

### 基准权重

| 指标 | 权重 | 侧重维度 |
|---|---|---|
| \( w_{TDT} \) 总晚点 | 0.20 | 效率 |
| \( w_{ADT} \) 平均晚点 | 0.10 | 效率 |
| \( w_{TPMD} \) 旅客延误 | 0.20 | 旅客 |
| \( w_{TAP} \) 受影响旅客 | 0.15 | 旅客 |
| \( w_{VFD} \) 公平性 | 0.10 | 公平 |
| \( w_{SD} \) 调整幅度 | 0.10 | 稳定性 |
| \( w_{SRT} \) 恢复时间 | 0.15 | 稳定性 |

---

## 七、场景特化

### 场景1：单列车晚点

初始扰动约束：

$$ D_{fault, s_{fault}}^{adj} = D_{fault, s_{fault}}^{plan} + \delta $$

其中 \( \delta \in [15, 30] \) 为初始晚点分钟数。

方案生成策略：
- **策略A（延迟传播）**：所有后续车站直接平移延迟
- **策略B（压缩停站）**：压缩停站时间至 2 分钟最小，吸收延迟
- **策略C（扣停冲突）**：扣停冲突列车，优先保证晚点车通过

### 场景2：区间临时限速

限速后最小运行时间（三阶段运动学）：

**减速阶段**：
$$ d_{dec} = \frac{V_{max}^2 - V_{limit}^2}{2a_{dec}}, \quad t_{dec} = \frac{V_{max} - V_{limit}}{a_{dec}} $$

**加速阶段**：
$$ d_{acc} = \frac{V_{max}^2 - V_{limit}^2}{2a_{acc}}, \quad t_{acc} = \frac{V_{max} - V_{limit}}{a_{acc}} $$

**匀速阶段**：
$$ t_{cruise} = \frac{d_{cruise}}{V_{limit}} $$

**新最小运行时间**：
$$ T_{run}^{new} = t_{dec} + t_{cruise} + t_{acc} $$

动力学参数：
- \( V_{max} = 350 \) km/h（线路最高速度）
- \( V_{limit} \) 为限速值（如 160 km/h）
- \( a_{dec} = 3000 \) km/h²（平均减速度）
- \( a_{acc} = 2000 \) km/h²（平均加速度）

# RailGPT 队友部署指南

## 一、你需要装什么

### 1.1 Ollama

去 https://ollama.com 下载，双击安装。安装完系统托盘会出现 Ollama 图标。

### 1.2 Python

确保电脑有 Python 3.10 或以上。终端输入 `python --version` 验证。

### 1.3 Chrome 浏览器（可选）

有 Chrome 的话窗口更好看。没有也能用，会自动降级为普通浏览器。

---

## 二、下载大模型

打开终端（Win+R → 输入 `cmd` → 回车），依次运行：

```bash
ollama pull qwen2.5:latest
ollama pull nomic-embed-text
```

第一个是对话大模型（约 4.7GB），第二个是嵌入模型（约 274MB）。下载完出现 `success` 即可。

验证模型已就绪：

```bash
ollama list
```

应看到 `qwen2.5:latest` 和 `nomic-embed-text:latest`。

> 如果电脑性能较弱（轻薄本/集成显卡），qwen2.5:latest 可能会有单次回复 1-2 分钟的延迟，属于正常现象。
> 如果想加速，可改用 `qwen2.5:3b`（约 2GB），但回复质量会降低。

---

## 三、拿到项目代码

### 方式 A：从 Git 拉取

```bash
git clone https://github.com/Cynthia-Shao/railgpt-project.git
cd railgpt-project
```

### 方式 B：接收压缩包

解压到本地某个目录（比如 `D:\RailGPT\railgpt-project`）。

---

## 四、安装 Python 依赖

终端进入项目目录后：

```bash
pip install flask flask-cors
```

---

## 五、修改配置

### 5.1 必改：项目路径

用记事本打开项目根目录下的 `RailGPT.bat`，把第二行的路径改成你自己的：

```bat
cd /d "你的项目目录路径"
```

例如你的项目放在 `E:\railgpt-project`，就改成：

```bat
cd /d "E:\railgpt-project"
```

### 5.2 可选：换模型

用记事本打开 `.env` 文件。如果你下载的模型名不同，修改这两行：

```
RAILGPT_LLM_MODEL=你的对话模型名
RAILGPT_EMBEDDING_MODEL=你的嵌入模型名
```

其他配置一般不用改。

---

## 六、启动

**双击 `RailGPT.bat`。**

会弹出两个黑色命令行窗口（不要关），几秒后自动打开应用窗口。首次启动需要计算嵌入向量（约 30 秒），之后每次启动秒开。

---

## 七、常见问题

### Q1：打开后显示 "Backend not running"

说明后端没启动成功。检查：
- 后端窗口是否显示 `Running on http://127.0.0.1:8000`
- 如果显示 `Loading knowledge base...` 之后报错，截图发给项目负责人

### Q2：能打开但发送问题很久没反应

模型在 CPU 上推理需要时间，最长可能 5-10 分钟。看到 `Processing...` 就是在跑，耐心等。

### Q3：第一次启动卡在 "Computing embeddings"

首次启动需要为 1000+ 个知识片段计算嵌入向量，约 30 秒。等待即可，后续重启秒开。

### Q4：模型完全跑不动，一直超时

你的电脑 CPU 可能带不动 7.6B 模型。改用轻量模型：
```bash
ollama pull qwen2.5:3b
```
然后把 `.env` 里的 `RAILGPT_LLM_MODEL` 改为 `qwen2.5:3b`，重启项目。

### Q5：端口被占用

如果报错 `address already in use` 或端口冲突，关掉所有 Python 进程后重试：
```
Ctrl+Shift+Esc → 搜索 python → 结束任务
```

# AGENTS

## 项目目标

RailGPT 是一个铁路调度辅助决策系统竞赛项目。系统需要结合铁路调度规则、历史案例、优化算法与大模型能力，为调度员生成多个可比较、可调整、可解释的调度方案。

## 团队分工

- 你负责：优化算法模块、大模型调用、Hybrid Retrieval、多库结构设计与后端核心能力
- 刘同学负责：前端交互界面，方案展示与人机交互
- 包同学负责：测试框架、测试执行、评分、报告生成

## 目录约定

- 前端代码放在 `apps/frontend/`
- 后端代码放在 `apps/backend/`
- 共享核心库放在 `railgpt_core/`
- 数据目录放在 `data/`

## 多库结构约定

- `data/rules/hard_rules/`：强规则库，属于硬约束，任何情况下都不能违反
- `data/rules/soft_rules/`：普通规则库，属于软约束，用于优化、解释和排序
- `data/scenarios/`：调度场景与测试输入
- `data/cases/`：历史案例与参考方案

## 共享库使用约定

- `railgpt_core/` 中的模型、知识库、检索、算法和工具模块应作为全项目统一复用的公共能力
- 后端 API、测试代码、报告代码应优先复用 `railgpt_core/`，避免重复实现
- 强规则相关逻辑必须在共享库中统一维护，不能分散在多个模块中各自实现

## 代码规范

- 统一使用 Python
- 遵循 PEP 8
- 新代码必须带 Type Hints
- 数据模型优先使用 Pydantic
- 模块职责保持单一，避免把检索、算法、提示词和 API 路由混写在一个文件里

## Commit 规范

- 提交信息尽量清晰，推荐使用：
  - `feat: ...`
  - `fix: ...`
  - `refactor: ...`
  - `docs: ...`
  - `test: ...`

## 测试要求

- 新增共享库能力时，优先补充 `pytest` 测试
- 强规则加载、规则分类、基础检索逻辑必须可测试
- 影响方案生成的逻辑修改后，应补充最小可复现测试样例

## 禁止事项

- 不得绕过强规则直接输出方案
- 不得在前端或临时脚本中硬编码核心业务规则，核心规则必须沉淀到共享库或数据目录
- 不得提交包含真实密钥、令牌或本地绝对私密路径的代码
- 不得复制粘贴多份相似业务逻辑而不抽取共享模块
